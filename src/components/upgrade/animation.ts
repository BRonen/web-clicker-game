import { keyframes } from "@stitches/react"

export const popup = keyframes({
    '0%': {
      opacity: '0%',
      transform: 'scale(0)',
    },
    '100%': {
      opacity: '100%',
      transform: 'scale(1)',
    }
})
